{
  "type": "object",
  "required": ["decision"],
  "properties": {
    "decision": {"type": "string", "enum": ["pass", "fail"]},
    "blocking_findings": {"type": "array", "items": {"type": "string"}}
  }
}
